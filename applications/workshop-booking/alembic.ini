[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
path_separator = os

# Connection credentials come exclusively from PG* environment variables.
# This file deliberately contains no sqlalchemy.url or passwords.
