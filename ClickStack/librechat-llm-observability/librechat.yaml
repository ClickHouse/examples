# Configuration version (required)
version: "1.2.1"        # ← must be a string

# Cache settings: Set to true to enable caching
cache: true

mcpServers:
  clickhouse-playground:
    type: sse
    url: http://host.docker.internal:8001/sse
